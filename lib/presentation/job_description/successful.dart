import 'package:flutter/material.dart';

class SuccessfulJobScreen extends StatelessWidget{
  const SuccessfulJobScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }

}